# Before run
(execute once)
```bash=
cd backend
./docker-build.sh
```
# run
```bash=
cd backend
./run.sh
```
#### Don't forget to give the "frontend" dir to the players!
# rebuild
```bash
docker rmi handicar-inside
```
